# Core export pipeline
pycryptodome
jinja2
zstandard
pilk
SpeechRecognition

# Analytics
numpy

# AI search (semantic search + RAG)
# Pin to 3.x — v5.x has a bug loading text-only models on Windows
sentence-transformers==3.4.1

# AI summaries — install one or both:
ollama          # local LLM via Ollama (https://ollama.com)
openai          # OpenAI API (set OPENAI_API_KEY env var)
